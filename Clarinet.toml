[project]
name = "peer-amm-curator"
authors = ["Peer AMM Curator Team"]
description = "A decentralized protocol for curated automated market-making and asset management"
telemetry = false
requirements = []
[contracts.curator-registry]
path = "contracts/curator-registry.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
